import styled from "styled-components";

const Footer = () => {
  return (
    <FooterStyle>
      <footer>copyright(c) 2025. bookstore</footer>
    </FooterStyle>
  );
};

const FooterStyle = styled.div`
  font-size: 14px;
`

export default Footer;
