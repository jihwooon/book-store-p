import styled from "styled-components";

const BooksFilter = () => {
  return (
    <BooksFilterStyle>
      <div>BooksFilter</div>
    </BooksFilterStyle>
  );
};

const BooksFilterStyle = styled.div``

export default BooksFilter;
